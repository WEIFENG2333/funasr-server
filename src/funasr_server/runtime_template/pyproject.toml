# This file is a template. The actual pyproject.toml used at runtime
# is dynamically generated by installer.py with the correct PyTorch
# index (CPU vs CUDA) based on GPU detection.
#
# See installer.py:_generate_pyproject() for details.

[project]
name = "funasr-server-runtime"
version = "0.1.0"
requires-python = ">=3.10,<3.13"
dependencies = [
    "funasr @ git+https://github.com/modelscope/FunASR.git",
    "modelscope",
    "huggingface_hub",
    "transformers",
    "tiktoken",
    "torch>=2.0.0",
    "torchaudio>=2.0.0",
    "uvicorn>=0.30.0",
    "starlette>=0.37.0",
]
